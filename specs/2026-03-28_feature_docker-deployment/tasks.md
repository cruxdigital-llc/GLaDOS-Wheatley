# Tasks: Docker & Deployment

- [x] 1. Finalize multi-stage Dockerfile (base, build, production, development)
- [x] 2. Add health check to production stage
- [x] 3. Update docker-compose.yml for local sidecar with all needed volume mounts
- [x] 4. Startup validation already in factory.ts (fail fast on missing env vars)
- [x] 5. CORS defaults to localhost:5173 for dev, configurable via CORS_ORIGIN
- [ ] 6. Run all tests via Docker — verify passing
